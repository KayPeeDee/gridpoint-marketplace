{
  "version": 2,
  "builds": [
    {
      "src": "dist/**",
      "use": "@vercel/static"
    }
  ],
  "install": {
    "include": [
      "^package.json",
      "^npm.lock"
    ],
    "command": "npm install --legacy-peer-deps"
  }
}
